<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.worldwide.practice">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <application
      android:name=".App"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <activity android:name=".feature.AutoCompleteActivity"/>

    <activity android:name=".feature.BufferClickActivity"/>
    <activity android:name=".feature.ConcurrencyWithSchedulersDemoActivity"/>
    <activity android:name=".feature.DataBindingActivity"/>
    <activity android:name=".feature.FormValidationActivity"/>
    <activity android:name=".feature.NetworkRequestActivity"/>
    <activity android:name=".feature.PollingDataActivity"/>

    <activity android:name=".feature.LauncherActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>

    </activity>
    <activity android:name=".feature.CheckInternetWorking"/>


  </application>

</manifest>